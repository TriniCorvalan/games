<%= form_with(model: game, local: true) do |form| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :rules , "Rules" %>
    <%= form.text_area :rules %>
    <%= form.file_field :pdf_rules %>
  </div>

  <div class="field">
    <%= form.label :box_image , "Box" %>
    <%= form.file_field :box_image %>
  </div>

  <div class="field">
    <%= form.label :parts_images , "Parts" %>
    <%= form.file_field :parts_images, multiple: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
